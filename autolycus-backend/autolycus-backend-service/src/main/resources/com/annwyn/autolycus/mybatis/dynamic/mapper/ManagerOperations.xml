<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.annwyn.autolycus.mybatis.dynamic.mapper.ManagerOperations">

    <select id="searchByMail" resultType="com.annwyn.autolycus.mybatis.dynamic.model.ManagerModel">
        select mmsm.*, mmsr.role_name
        from mms_manager as mmsm
        left join mms_role as mmsr on mmsm.role_id = mmsr.role_id
        where mmsm.manager_mail = #{managerMail}
    </select>


</mapper>